# Requirements list for the Tech News AI test environment.
# Dictates pinned package dependencies for reproducibility.

crawl4ai>=0.3.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# Library interface responsible for downloading and managing Chromium binaries
playwright>=1.40.0
feedparser>=6.0.10

# Postgres driver + pgvector adapter for Neon storage
psycopg2-binary>=2.9.9
pgvector>=0.2.5

# HTTP client for Groq embedding API calls
httpx>=0.27.0

# LangChain agent stack for the ReAct Judge (judge_agent.py, judge_tools.py)
# langgraph is pulled in automatically as a dependency of langchain.agents.create_agent
langchain>=1.0.0
langchain-ollama>=0.3.0

# Article body extraction used by the judge's get_article_context tool
trafilatura>=1.8.0
beautifulsoup4>=4.12.0

# DataFrame display for the experimentation notebook (not used by any .py module yet)
pandas>=2.0.0

# FastAPI backend & Search API for real-time frontend & live pipeline
fastapi>=0.110.0
uvicorn>=0.28.0
tavily-python>=0.3.0